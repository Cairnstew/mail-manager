[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[project]
dependencies = [
    "accelerate>=1.13.0",
    "bitsandbytes>=0.49.2",
    "transformers>=5.4.0",
]
description = "Generated from Nix"
name = "mail-manager"
requires-python = ">=3.12"
version = "0.1.0"

[project.scripts]
mail-manager = "mail_manager.cli:main"
